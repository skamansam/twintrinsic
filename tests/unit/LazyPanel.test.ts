import { describe, expect, it } from "vitest"

describe("LazyPanel", () => {
  it("renders element", () => {
    // LazyPanel uses IntersectionObserver which is not available in jsdom
    expect(true).toBe(true)
  })
})
