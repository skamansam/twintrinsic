// Type declarations for the interestfor polyfill package.
// The package ships no .d.ts file, so we declare it here.

declare module "interestfor" {
  const content: unknown
  export default content
}
